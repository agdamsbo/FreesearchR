# Observations filter test wrapper

Observations filter test wrapper

## Usage

``` r
obs_filter_validate(before, after)
```

## Arguments

- before:

  data before

- after:

  data after

## Value

vector
